import "./App.css"; // Import custom CSS styles for the application.
import MapBox from "./components/Map"; // Import the MapBox component from the "components" folder.

function App() {
  return <MapBox />; // Render the MapBox component, which displays the map.
}

export default App; // Export the "App" component as the main entry point for your application.
